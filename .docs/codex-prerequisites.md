# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running In2Peta ADE (for example via API key or ChatGPT auth supported by Codex).
- In2Peta ADE starts the server via `codex app-server` per session.
